@ Install the Grunt Command Line for NPM 
    
    // Allows you to run command line grunt commands

        npm install -g grunt-cli

    // Install Project Grunt Package
    
        npm install grunt –save-dev

@ Overview

    // NPM is the Project Manager, 
    // Bower is the Package Manager, 
    // Grunt is the Build Manager

    // Grunt is Dependent on the Project
        // listed as Project Dependency
        // Dependency listed in Project.json

// NPM file now contains link to Grunt File 

    'package.json' 

        "devDependencies": { 
            "grunt": "0.4.1" 
        }

